# Spring application name
spring.application.name=de3mo


# OAuth2 Client settings for GitHub and Google
spring.security.oauth2.client.registration.github.client-id=${GITHUB_CLIENT_ID}
spring.security.oauth2.client.registration.github.client-secret=${GITHUB_CLIENT_SECRET}
spring.security.oauth2.client.registration.github.scope=read:user,user:email

spring.security.oauth2.client.registration.google.client-id=${GOOGLE_CLIENT_ID}
spring.security.oauth2.client.registration.google.client-secret=${GOOGLE_CLIENT_SECRET}
spring.security.oauth2.client.registration.google.scope=profile,email

# JWT settings
app.jwt.secret=v9y$B&E)H@MbQeThWmZq4t7w!z%C*F-JaNdRfUjXn2r5u8x/A?D(G+KbPeShVkYp
app.jwt.expiration.minutes=10

# OAuth2 redirect URI
app.oauth2.redirectUri=http://localhost:3000/oauth2/redirect

# CORS allowed origins
app.cors.allowed-origins=http://localhost:3001

# Logging levels
logging.level.org.springframework.security=DEBUG
# logging.level.org.hibernate.SQL=DEBUG

# MySQL Configuration
spring.datasource.url=jdbc:mysql://localhost:3306/cancerdb
spring.datasource.username=root
spring.datasource.password=Qazwsx@123
# JPA/Hibernate properties
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true

#spring.datasource.driver-class-name=com.mysql.cj.jdbc.Driver
spring.datasource.driver-class-name=com.mysql.cj.jdbc.Driver

spring.jpa.database-platform=org.hibernate.dialect.MySQLDialect

# Cancer Detection Service Configuration
cancer.detection.service.url=http://localhost:5000
